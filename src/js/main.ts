/**
 * 
 * BATTLESHITS
 *  plz "npm i"
 * 
 */

module MyModule {
    export const Init = () => {
        console.log('Hello world!');
    }


    
     //     Make function(s) for each point
     
     // 1) Create battle grid (variable or static, doesnt matter)

     // 2) Create system that allows certain ships on the grid that can be rotated

     // 3) make one grid -> then make another, for enemy

     // 4) when ships are assigned, begin battle (first player chooses where to strike. If misses - next player goes, if hit - go again)

     


}


$(document).ready( () => {
    MyModule.Init();

})
